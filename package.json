{
  "name": "react-navigation-menu",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "classnames": "^2.2.6",
    "lodash": "^4.17.10",
    "normalize.css": "^8.0.0",
    "react": "^16.5.0",
    "react-dom": "^16.5.0",
    "react-scripts": "1.1.5",
    "react-transition-group": "^2.4.0"
  },
  "lint-staged": {
    "src/**/*.{js,jsx,json}": [
      "prettier --single-quote --write",
      "git add"
    ]
  },
  "scripts": {
    "precommit": "lint-staged",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "lint:css": "stylelint \"src/**/*.css\"",
    "prettier-watch": "onchange 'src/**/*.{js,jsx,json}' -- prettier --write {{changed}}",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "lint-staged": "^7.2.2",
    "onchange": "^4.1.0",
    "prettier": "^1.14.2",
    "stylelint": "^9.5.0",
    "stylelint-config-standard": "^18.2.0"
  }
}
